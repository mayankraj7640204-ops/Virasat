const translations = {
  en: {
    logo: "INDIA", navHome: "Home", navAbout: "About", navGallery: "Gallery", navFeatures: "Features", heroTitle: "VIRASAT",
    introText: "A vibrant tapestry of ancient stone, sweeping landscapes, and unbroken living traditions shaping a breathtaking cultural journey across the Indian subcontinent.",
    tag1: "Ancient Heritage", tag2: "Living Arts", tag3: "Spiritual Journey",
    panel1Title: "Stones that speak.", panel1Desc: "India's monuments are architectural compasses bridging millennia of art, astronomy, and dynastic history.",
    panel2Title: "The living pulse.", panel2Desc: "Culture here is not just observed in museums; it is experienced in every street, festival, and artisan's craft.",
    card1Kicker: "Timeless Architecture", card1Title: "Temples of Hampi", card1Desc: "Stone chariots and monolithic ruins echoing the glory of the Vijayanagara Empire.",
    card2Kicker: "Spiritual Heart", card2Title: "Varanasi Ghats", card2Desc: "The world's oldest living city, where ancient rituals meet the sacred Ganges.",
    card3Kicker: "Royal Legacy", card3Title: "Rajputana Forts", card3Desc: "Impregnable hill forts in Rajasthan featuring intricate palaces and sweeping views.",
    card4Kicker: "Living Arts", card4Title: "Classical Traditions", card4Desc: "From Bharatanatyam to ancient copper craft, a culture that lives and breathes today.",
    card5Kicker: "Natural Heritage", card5Title: "Kerala Backwaters", card5Desc: "An interconnected network of ancient trade routes and serene ecological harmony.",
    chaptersSubtitle: "VIRASAT JOURNEY", chaptersTitle: "EXPLORE THE CHAPTERS", exploreBtn: "EXPLORE →",
    chap1Num: "CHAPTER 01", chap1Title: "The Archive of Monuments", chap1Desc: "Interactive 3D structural mapping and historical reconstruction of India's architectural marvels.",
    chap2Num: "CHAPTER 02", chap2Title: "Kathakar AI Guide", chap2Desc: "Conversational lore and historical narratives powered by Google Gemini, speaking fluent local folklore.",
    chap3Num: "CHAPTER 03", chap3Title: "Lipika Script Lens", chap3Desc: "Real-time optical character recognition decoding ancient Brahmi, Pali, and Sanskrit stone inscriptions.",
    chap4Num: "CHAPTER 04", chap4Title: "Blockchain Yatra Passports", chap4Desc: "Cryptographic digital stamps and NFTs verifying your journey across India's cultural corridors.",
    chap5Num: "CHAPTER 05", chap5Title: "Pratidhvani Spatial Audio", chap5Desc: "3D binaural acoustic reconstruction simulating classical royal courts and temple chants on location.",
    chap6Num: "CHAPTER 06", chap6Title: "Vyanjan Spice Route Scanner", chap6Desc: "AI computer vision breaking down regional culinary history and ancient spice trade origins.",
    chap7Num: "CHAPTER 07", chap7Title: "Parampara Lore Quests", chap7Desc: "Geo-fenced gamified heritage exploration unlocking digital rewards and local artisan discounts.",
    chap8Num: "CHAPTER 08", chap8Title: "Kala-Kriti Digital Try-On", chap8Desc: "AR body segmentation allowing tourists to digitally drape regional handloom textiles and weaves.",
    getStarted: "Get Started",
    authClose: "[ CLOSE ]", authWork: "Work", authStudio: "Studio", authLab: "Lab", authContact: "Contact",
    authJoinTitle: "Join Virasat", authJoinDesc: "Unlock personalized heritage journeys and artifacts.", authEmail: "EMAIL ADDRESS",
    authCookieTitle: "A long time ago, in a browser far far away...", authCookieDesc: "We use cookies to understand how the site performs – nothing sinister, no dark side stuff.",
    authCookieAccept: "MAY THE COOKIES BE WITH YOU", authCookieDecline: "I'LL PASS, THANKS",
    authPrivacy: "PRIVACY POLICY", authSupport: "SUPPORT"
  },
  hi: {
    logo: "भारत", navHome: "मुख्य पृष्ठ", navAbout: "हमारे बारे में", navGallery: "गैलरी", navFeatures: "विशेषताएं", heroTitle: "विरासत",
    introText: "प्राचीन पत्थरों, विस्तृत परिदृश्यों और अखंड जीवंत परंपराओं की एक जीवंत टेपेस्ट्री, जो भारतीय उपमहाद्वीप में एक लुभावनी सांस्कृतिक यात्रा को आकार देती है।",
    tag1: "प्राचीन विरासत", tag2: "जीवित कलाएं", tag3: "आध्यात्मिक यात्रा",
    panel1Title: "पत्थर जो बोलते हैं।", panel1Desc: "भारत के स्मारक स्थापत्य कंपास हैं जो कला, खगोल विज्ञान और राजवंशीय इतिहास की सहस्राब्दी को जोड़ते हैं।",
    panel2Title: "जीवंत स्पंदन।", panel2Desc: "यहाँ संस्कृति केवल संग्रहालयों में नहीं देखी जाती; यह हर सड़क, त्योहार और कारीगर के शिल्प में अनुभव की जाती है।",
    card1Kicker: "कालातीत वास्तुकला", card1Title: "हम्पी के मंदिर", card1Desc: "विजयनगर साम्राज्य की महिमा को दर्शाते पत्थर के रथ और अखंड खंडहर।",
    card2Kicker: "आध्यात्मिक हृदय", card2Title: "वाराणसी के घाट", card2Desc: "दुनिया का सबसे पुराना जीवित शहर, जहां प्राचीन अनुष्ठान पवित्र गंगा से मिलते हैं।",
    card3Kicker: "शाही विरासत", card3Title: "राजपूताना किले", card3Desc: "राजस्थान के अभेद्य पहाड़ी किले जिनमें जटिल महल और मनोरम दृश्य हैं।",
    card4Kicker: "जीवित कलाएं", card4Title: "शास्त्रीय परंपराएं", card4Desc: "भरतनाट्यम से लेकर प्राचीन तांबे के शिल्प तक, एक ऐसी संस्कृति जो आज भी जीवित है और सांस लेती है।",
    card5Kicker: "प्राकृतिक विरासत", card5Title: "केरल बैकवाटर्स", card5Desc: "प्राचीन व्यापार मार्गों और शांत पारिस्थितिक सद्भाव का एक परस्पर नेटवर्क।",
    chaptersSubtitle: "विरासत यात्रा", chaptersTitle: "अध्याय एक्सप्लोर करें", exploreBtn: "एक्सप्लोर करें →",
    chap1Num: "अध्याय 01", chap1Title: "स्मारकों का पुरालेख", chap1Desc: "भारत के वास्तुशिल्प चमत्कारों का इंटरैक्टिव 3डी संरचनात्मक मानचित्रण और ऐतिहासिक पुनर्निर्माण।",
    chap2Num: "अध्याय 02", chap2Title: "कथाकार एआई गाइड", chap2Desc: "Google Gemini द्वारा संचालित संवादी विद्या और ऐतिहासिक आख्यान, जो स्थानीय लोककथाएं बोलते हैं।",
    chap3Num: "अध्याय 03", chap3Title: "लिपि स्क्रिप्ट लेंस", chap3Desc: "प्राचीन ब्राह्मी, पाली और संस्कृत पत्थर के शिलालेखों को डिकोड करने वाली वास्तविक समय की ऑप्टिकल चरित्र मान्यता।",
    chap4Num: "अध्याय 04", chap4Title: "ब्लॉकचेन यात्रा पासपोर्ट", chap4Desc: "भारत के सांस्कृतिक गलियारों में आपकी यात्रा की पुष्टि करने वाले क्रिप्टोग्राफिक डिजिटल टिकट और एनएफटी।",
    chap5Num: "अध्याय 05", chap5Title: "प्रतिध्वनि स्थानिक ऑडियो", chap5Desc: "3डी बाइनॉरल ध्वनिक पुनर्निर्माण शास्त्रीय दरबारों और मंदिर के मंत्रों का अनुकरण करता है।",
    chap6Num: "अध्याय 06", chap6Title: "व्यंजन मसाला मार्ग स्कैनर", chap6Desc: "एआई कंप्यूटर विज़न क्षेत्रीय पाक इतिहास और प्राचीन मसाला व्यापार की उत्पत्ति को डिकोड करता है।",
    chap7Num: "अध्याय 07", chap7Title: "परंपरा विद्या अन्वेषण", chap7Desc: "जियो-फेंस्ड गामिफाइड हेरिटेज एक्सप्लोरेशन डिजिटल रिवॉर्ड्स और स्थानीय कारीगर छूट अनलॉक करता है।",
    chap8Num: "अध्याय 08", chap8Title: "कला-कृति डिजिटल ट्राई-ऑन", chap8Desc: "एआर बॉडी सेगमेंटेशन जिससे पर्यटक क्षेत्रीय हथकरघा वस्त्रों को डिजिटल रूप से पहन सकते हैं।",
    getStarted: "शुरू करें",
    authClose: "[ बंद करें ]", authWork: "कार्य", authStudio: "स्टूडियो", authLab: "लैब", authContact: "संपर्क",
    authJoinTitle: "विरासत से जुड़ें", authJoinDesc: "व्यक्तिगत विरासत यात्राओं और कलाकृतियों को अनलॉक करें।", authEmail: "ईमेल पता",
    authCookieTitle: "बहुत समय पहले, एक बहुत दूर के ब्राउज़र में...", authCookieDesc: "हम यह समझने के लिए कुकीज़ का उपयोग करते हैं कि साइट कैसा प्रदर्शन करती है - कुछ भी भयावह नहीं, कोई डार्क साइड का काम नहीं।",
    authCookieAccept: "कुकीज़ आपके साथ रहें", authCookieDecline: "मैं पास करूँगा, धन्यवाद",
    authPrivacy: "गोपनीयता नीति", authSupport: "सहायता"
  },
  kn: {
    logo: "ಭಾರತ", navHome: "ಮುಖಪುಟ", navAbout: "ನಮ್ಮ ಬಗ್ಗೆ", navGallery: "ಗ್ಯಾಲರಿ", navFeatures: "ವೈಶಿಷ್ಟ್ಯಗಳು", heroTitle: "ವಿರಾಸತ್",
    introText: "ಭಾರತೀಯ ಉಪಖಂಡದಾದ್ಯಂತ ಉಸಿರುಕಟ್ಟುವ ಸಾಂಸ್ಕೃತಿಕ ಪ್ರಯಾಣವನ್ನು ರೂಪಿಸುವ ಪ್ರಾಚೀನ ಕಲ್ಲುಗಳು, ವಿಸ್ತಾರವಾದ ಭೂದೃಶ್ಯಗಳು ಮತ್ತು ಮುರಿಯದ ಜೀವಂತ ಸಂಪ್ರದಾಯಗಳ ರೋಮಾಂಚಕ ವಸ್ತ್ರ.",
    tag1: "ಪ್ರಾಚೀನ ಪರಂಪರೆ", tag2: "ಜೀವಂತ ಕಲೆಗಳು", tag3: "ಆಧ್ಯಾತ್ಮಿಕ ಪ್ರಯಾಣ",
    panel1Title: "ಮಾತನಾಡುವ ಕಲ್ಲುಗಳು.", panel1Desc: "ಭಾರತದ ಸ್ಮಾರಕಗಳು ಕಲೆ, ಖಗೋಳಶಾಸ್ತ್ರ ಮತ್ತು ರಾಜವಂಶದ ಇತಿಹಾಸವನ್ನು ಬೆಸೆಯುವ ವಾಸ್ತುಶಿಲ್ಪದ ದಿಕ್ಸೂಚಿಗಳಾಗಿವೆ.",
    panel2Title: "ಜೀವಂತ ನಾಡಿ.", panel2Desc: "ಇಲ್ಲಿನ ಸಂಸ್ಕೃತಿಯನ್ನು ವಸ್ತುಸಂಗ್ರಹಾಲಯಗಳಲ್ಲಿ ಮಾತ್ರ ನೋಡಲಾಗುವುದಿಲ್ಲ; ಪ್ರತಿಯೊಂದು ಬೀದಿ, ಹಬ್ಬ ಮತ್ತು ಕುಶಲಕರ್ಮಿಗಳ ಕಲೆಯಲ್ಲಿ ಇದನ್ನು ಅನುಭವಿಸಲಾಗುತ್ತದೆ.",
    card1Kicker: "ಕಾಲಾತೀತ ವಾಸ್ತುಶಿಲ್ಪ", card1Title: "ಹಂಪಿಯ ದೇವಾಲಯಗಳು", card1Desc: "ವಿಜಯನಗರ ಸಾಮ್ರಾಜ್ಯದ ವೈಭವವನ್ನು ಪ್ರತಿಬಿಂಬಿಸುವ ಕಲ್ಲಿನ ರಥಗಳು ಮತ್ತು ಏಕಶಿಲಾ ಅವಶೇಷಗಳು.",
    card2Kicker: "ಆಧ್ಯಾತ್ಮಿಕ ಹೃದಯ", card2Title: "ವಾರಣಾಸಿ ಘಾಟ್ಗಳು", card2Desc: "ಪ್ರಾಚೀನ ಆಚರಣೆಗಳು ಪವಿತ್ರ ಗಂಗೆಯನ್ನು ಸಂಧಿಸುವ ವಿಶ್ವದ ಅತ್ಯಂತ ಹಳೆಯ ಜೀವಂತ ನಗರ.",
    card3Kicker: "ರಾಜ ಪರಂಪರೆ", card3Title: "ರಜಪೂತಾನ ಕೋಟೆಗಳು", card3Desc: "ಸಂಕೀರ್ಣವಾದ ಅರಮನೆಗಳು ಮತ್ತು ವಿಸ್ತಾರವಾದ ನೋಟಗಳನ್ನು ಒಳಗೊಂಡಿರುವ ರಾಜಸ್ಥಾನದ ಭೇದಿಸಲಾಗದ ಬೆಟ್ಟದ ಕೋಟೆಗಳು.",
    card4Kicker: "ಜೀವಂತ ಕಲೆಗಳು", card4Title: "ಶಾಸ್ತ್ರೀಯ ಸಂಪ್ರದಾಯಗಳು", card4Desc: "ಭರತನಾಟ್ಯದಿಂದ ಪ್ರಾಚೀನ ತಾಮ್ರದ ಕರಕುಶಲತೆಯವರೆಗೆ, ಇಂದಿಗೂ ಜೀವಿಸುವ ಮತ್ತು ಉಸಿರಾಡುವ ಸಂಸ್ಕೃತಿ.",
    card5Kicker: "ನೈಸರ್ಗಿಕ ಪರಂಪರೆ", card5Title: "ಕೇರಳ ಬ್ಯಾಕ್ವಾಟರ್ಸ್", card5Desc: "ಪ್ರಾಚೀನ ವ್ಯಾಪಾರ ಮಾರ್ಗಗಳು ಮತ್ತು ಪ್ರಶಾಂತ ಪರಿಸರ ಸಾಮರಸ್ಯದ ಪರಸ್ಪರ ಜಾಲ.",
    chaptersSubtitle: "ವಿರಾಸತ್ ಪ್ರಯಾಣ", chaptersTitle: "ಅಧ್ಯಾಯಗಳನ್ನು ಅನ್ವೇಷಿಸಿ", exploreBtn: "ಅನ್ವೇಷಿಸಿ →",
    chap1Num: "ಅಧ್ಯಾಯ 01", chap1Title: "ಸ್ಮಾರಕಗಳ ಆರ್ಕೈವ್", chap1Desc: "ಭಾರತದ ವಾಸ್ತುಶಿಲ್ಪದ ಅದ್ಭುತಗಳ ಸಂವಾದಾತ್ಮಕ 3D ರಚನಾತ್ಮಕ ಮ್ಯಾಪಿಂಗ್ ಮತ್ತು ಐತಿಹಾಸಿಕ ಪುನರ್ನಿರ್ಮಾಣ.",
    chap2Num: "ಅಧ್ಯಾಯ 02", chap2Title: "ಕಥಾಕರ್ AI ಮಾರ್ಗದರ್ಶಿ", chap2Desc: "ಸ್ಥಳೀಯ ಜಾನಪದವನ್ನು ನಿರರ್ಗಳವಾಗಿ ಮಾತನಾಡುವ Google Gemini ಯಿಂದ ನಡೆಸಲ್ಪಡುವ ಸಂವಾದಾತ್ಮಕ ಕಥೆ ಮತ್ತು ಐತಿಹಾಸಿಕ ನಿರೂಪಣೆಗಳು.",
    chap3Num: "ಅಧ್ಯಾಯ 03", chap3Title: "ಲಿಪಿಕಾ ಸ್ಕ್ರಿಪ್ಟ್ ಲೆನ್ಸ್", chap3Desc: "ಪ್ರಾಚೀನ ಬ್ರಾಹ್ಮಿ, ಪಾಲಿ ಮತ್ತು ಸಂಸ್ಕೃತ ಕಲ್ಲಿನ ಶಾಸನಗಳನ್ನು ಡಿಕೋಡ್ ಮಾಡುವ ನೈಜ-ಸಮಯದ ಆಪ್ಟಿಕಲ್ ಅಕ್ಷರ ಗುರುತಿಸುವಿಕೆ.",
    chap4Num: "ಅಧ್ಯಾಯ 04", chap4Title: "ಬ್ಲಾಕ್‌ಚೈನ್ ಯಾತ್ರಾ ಪಾಸ್‌ಪೋರ್ಟ್‌ಗಳು", chap4Desc: "ಭಾರತದ ಸಾಂಸ್ಕೃತಿಕ ಕಾರಿಡಾರ್‌ಗಳಾದ್ಯಂತ ನಿಮ್ಮ ಪ್ರಯಾಣವನ್ನು ಪರಿಶೀಲಿಸುವ ಕ್ರಿಪ್ಟೋಗ್ರಾಫಿಕ್ ಡಿಜಿಟಲ್ ಸ್ಟ್ಯಾಂಪ್‌ಗಳು ಮತ್ತು NFT ಗಳು.",
    chap5Num: "ಅಧ್ಯಾಯ 05", chap5Title: "ಪ್ರತಿಧ್ವನಿ ಪ್ರಾದೇಶಿಕ ಆಡಿಯೋ", chap5Desc: "3D ಬೈನಾಮರಲ್ ಅಕೌಸ್ಟಿಕ್ ಪುನರ್ನಿರ್ಮಾಣವು ಶಾಸ್ತ್ರೀಯ ರಾಜಮನೆತನದ ಆಸ್ಥಾನಗಳು ಮತ್ತು ದೇವಾಲಯದ ಮಂತ್ರಗಳನ್ನು ಅನುಕರಿಸುತ್ತದೆ.",
    chap6Num: "ಅಧ್ಯಾಯ 06", chap6Title: "ವ್ಯಂಜನ್ ಮಸಾಲೆ ಮಾರ್ಗ ಸ್ಕ್ಯಾನರ್", chap6Desc: "AI ಕಂಪ್ಯೂಟರ್ ದೃಷ್ಟಿ ಪ್ರಾದೇಶಿಕ ಪಾಕಶಾಲೆಯ ಇತಿಹಾಸ ಮತ್ತು ಪ್ರಾಚೀನ ಮಸಾಲೆ ವ್ಯಾಪಾರದ ಮೂಲವನ್ನು ಡಿಕೋಡ್ ಮಾಡುತ್ತದೆ.",
    chap7Num: "ಅಧ್ಯಾಯ 07", chap7Title: "ಪರಂಪರಾ ಇತಿಹಾಸದ ಕ್ವೆಸ್ಟ್‌ಗಳು", chap7Desc: "ಜಿಯೋ-ಫೆನ್ಸ್ಡ್ ಗ್ಯಾಮಿಫೈಡ್ ಹೆರಿಟೇಜ್ ಅನ್ವೇಷಣೆಯು ಡಿಜಿಟಲ್ ಪ್ರತಿಫಲಗಳು ಮತ್ತು ಸ್ಥಳೀಯ ಕುಶಲಕರ್ಮಿಗಳ ರಿಯಾಯಿತಿಗಳನ್ನು ಅನ್ಲಾಕ್ ಮಾಡುತ್ತದೆ.",
    chap8Num: "ಅಧ್ಯಾಯ 08", chap8Title: "ಕಲಾ-ಕೃತಿ ಡಿಜಿಟಲ್ ಟ್ರೈ-ಆನ್", chap8Desc: "ಪ್ರಾದೇಶಿಕ ಕೈಮಗ್ಗದ ಜವಳಿಗಳನ್ನು ಡಿಜಿಟಲ್ ಆಗಿ ಧರಿಸಲು ಪ್ರವಾಸಿಗರಿಗೆ ಅವಕಾಶ ನೀಡುವ AR ದೇಹ ವಿಭಾಗ.",
    getStarted: "ಪ್ರಾರಂಭಿಸಿ",
    authClose: "[ ಮುಚ್ಚಿ ]", authWork: "ಕೆಲಸ", authStudio: "ಸ್ಟುಡಿಯೋ", authLab: "ಲ್ಯಾಬ್", authContact: "ಸಂಪರ್ಕಿಸಿ",
    authJoinTitle: "ವಿರಾಸತ್ ಸೇರಿ", authJoinDesc: "ವೈಯಕ್ತಿಕಗೊಳಿಸಿದ ಪರಂಪರೆಯ ಪ್ರಯಾಣಗಳು ಮತ್ತು ಕಲಾಕೃತಿಗಳನ್ನು ಅನ್ಲಾಕ್ ಮಾಡಿ.", authEmail: "ಇಮೇಲ್ ವಿಳಾಸ",
    authCookieTitle: "ಬಹಳ ಹಿಂದೆಯೇ, ದೂರದ ಬ್ರೌಸರ್‌ನಲ್ಲಿ...", authCookieDesc: "ಸೈಟ್ ಹೇಗೆ ಕಾರ್ಯನಿರ್ವಹಿಸುತ್ತದೆ ಎಂಬುದನ್ನು ಅರ್ಥಮಾಡಿಕೊಳ್ಳಲು ನಾವು ಕುಕೀಗಳನ್ನು ಬಳಸುತ್ತೇವೆ - ಕೆಟ್ಟದ್ದೇನೂ ಇಲ್ಲ, ಯಾವುದೇ ಡಾರ್ಕ್ ಸೈಡ್ ವಿಷಯಗಳಿಲ್ಲ.",
    authCookieAccept: "ಕುಕೀಗಳು ನಿಮ್ಮೊಂದಿಗೆ ಇರಲಿ", authCookieDecline: "ನಾನು ಪಾಸ್ ಮಾಡುತ್ತೇನೆ, ಧನ್ಯವಾದಗಳು",
    authPrivacy: "ಗೌಪ್ಯತೆ ನೀತಿ", authSupport: "ಬೆಂಬಲ"
  },
  pa: {
    logo: "ਭਾਰਤ", navHome: "ਮੁੱਖ ਪੰਨਾ", navAbout: "ਸਾਡੇ ਬਾਰੇ", navGallery: "ਗੈਲਰੀ", navFeatures: "ਵਿਸ਼ੇਸ਼ਤਾਵਾਂ", heroTitle: "ਵਿਰਾਸਤ",
    introText: "ਪ੍ਰਾਚੀਨ ਪੱਥਰਾਂ, ਵਿਸ਼ਾਲ ਦ੍ਰਿਸ਼ਾਂ, ਅਤੇ ਅਟੁੱਟ ਜੀਵਤ ਪਰੰਪਰਾਵਾਂ ਦੀ ਇੱਕ ਜੀਵੰਤ ਟੇਪਸਟਰੀ ਜੋ ਭਾਰਤੀ ਉਪਮਹਾਂਦੀਪ ਵਿੱਚ ਇੱਕ ਸ਼ਾਨਦਾਰ ਸੱਭਿਆਚਾਰਕ ਯਾਤਰਾ ਨੂੰ ਰੂਪ ਦਿੰਦੀ ਹੈ।",
    tag1: "ਪ੍ਰਾਚੀਨ ਵਿਰਾਸਤ", tag2: "ਜੀਵਤ ਕਲਾਵਾਂ", tag3: "ਅਧਿਆਤਮਿਕ ਯਾਤਰਾ",
    panel1Title: "ਪੱਥਰ ਜੋ ਬੋਲਦੇ ਹਨ।", panel1Desc: "ਭਾਰਤ ਦੀਆਂ ਯਾਦਗਾਰਾਂ ਕਲਾ, ਖਗੋਲ ਵਿਗਿਆਨ ਅਤੇ ਰਾਜਵੰਸ਼ ਦੇ ਇਤਿਹਾਸ ਦੇ ਹਜ਼ਾਰਾਂ ਸਾਲਾਂ ਨੂੰ ਜੋੜਨ ਵਾਲੇ ਆਰਕੀਟੈਕਚਰਲ ਕੰਪਾਸ ਹਨ।",
    panel2Title: "ਜੀਵਤ ਨਬਜ਼।", panel2Desc: "ਇੱਥੇ ਸੱਭਿਆਚਾਰ ਸਿਰਫ਼ ਅਜਾਇਬ ਘਰਾਂ ਵਿੱਚ ਨਹੀਂ ਦੇਖਿਆ ਜਾਂਦਾ; ਇਹ ਹਰ ਗਲੀ, ਤਿਉਹਾਰ ਅਤੇ ਕਾਰੀਗਰਾਂ ਦੀ ਕਲਾ ਵਿੱਚ ਮਹਿਸੂਸ ਕੀਤਾ ਜਾਂਦਾ ਹੈ।",
    card1Kicker: "ਸਦੀਵੀ ਆਰਕੀਟੈਕਚਰ", card1Title: "ਹੰਪੀ ਦੇ ਮੰਦਰ", card1Desc: "ਵਿਜੇਨਗਰ ਸਾਮਰਾਜ ਦੀ ਸ਼ਾਨ ਨੂੰ ਦਰਸਾਉਂਦੇ ਪੱਥਰ ਦੇ ਰੱਥ ਅਤੇ ਅਖੰਡ ਖੰਡਰ।",
    card2Kicker: "ਅਧਿਆਤਮਿਕ ਦਿਲ", card2Title: "ਵਾਰਾਣਸੀ ਦੇ ਘਾਟ", card2Desc: "ਦੁਨੀਆ ਦਾ ਸਭ ਤੋਂ ਪੁਰਾਣਾ ਜੀਵਤ ਸ਼ਹਿਰ, ਜਿੱਥੇ ਪ੍ਰਾਚੀਨ ਰੀਤੀ-ਰਿਵਾਜ ਪਵਿੱਤਰ ਗੰਗਾ ਨੂੰ ਮਿਲਦੇ ਹਨ।",
    card3Kicker: "ਸ਼ਾਹੀ ਵਿਰਾਸਤ", card3Title: "ਰਾਜਪੂਤਾਨਾ ਕਿਲੇ", card3Desc: "ਰਾਜਸਥਾਨ ਦੇ ਅਭੇਦ ਪਹਾੜੀ ਕਿਲੇ ਜਿਨ੍ਹਾਂ ਵਿੱਚ ਗੁੰਝਲਦਾਰ ਮਹਿਲ ਅਤੇ ਸ਼ਾਨਦਾਰ ਦ੍ਰਿਸ਼ ਹਨ।",
    card4Kicker: "ਜੀਵਤ ਕਲਾਵਾਂ", card4Title: "ਕਲਾਸੀਕਲ ਪਰੰਪਰਾਵਾਂ", card4Desc: "ਭਰਤਨਾਟਿਅਮ ਤੋਂ ਲੈ ਕੇ ਪ੍ਰਾਚੀਨ ਤਾਂਬੇ ਦੀ ਕਲਾ ਤੱਕ, ਇੱਕ ਅਜਿਹਾ ਸੱਭਿਆਚਾਰ ਜੋ ਅੱਜ ਵੀ ਜਿਉਂਦਾ ਹੈ ਅਤੇ ਸਾਹ ਲੈਂਦਾ ਹੈ।",
    card5Kicker: "ਕੁਦਰਤੀ ਵਿਰਾਸਤ", card5Title: "ਕੇਰਲ ਬੈਕਵਾਟਰਸ", card5Desc: "ਪ੍ਰਾਚੀਨ ਵਪਾਰਕ ਮਾਰਗਾਂ ਅਤੇ ਸ਼ਾਂਤ ਵਾਤਾਵਰਣਕ ਸਦਭਾਵਨਾ ਦਾ ਆਪਸ ਵਿੱਚ ਜੁੜਿਆ ਨੈਟਵਰਕ।",
    chaptersSubtitle: "ਵਿਰਾਸਤ ਯਾਤਰਾ", chaptersTitle: "ਅਧਿਆਇ ਐਕਸਪਲੋਰ ਕਰੋ", exploreBtn: "ਐਕਸਪਲੋਰ ਕਰੋ →",
    chap1Num: "ਅਧਿਆਇ 01", chap1Title: "ਸਮਾਰਕਾਂ ਦਾ ਪੁਰਾਲੇਖ", chap1Desc: "ਭਾਰਤ ਦੇ ਆਰਕੀਟੈਕਚਰਲ ਅਜੂਬਿਆਂ ਦੀ ਇੰਟਰਐਕਟਿਵ 3D ਢਾਂਚਾਗਤ ਮੈਪਿੰਗ ਅਤੇ ਇਤਿਹਾਸਕ ਪੁਨਰ ਨਿਰਮਾਣ।",
    chap2Num: "ਅਧਿਆਇ 02", chap2Title: "ਕਥਾਕਾਰ ਏਆਈ ਗਾਈਡ", chap2Desc: "ਗੂਗਲ ਜੇਮਿਨੀ ਦੁਆਰਾ ਸੰਚਾਲਿਤ ਗੱਲਬਾਤ ਦੀ ਵਿਦਿਆ ਅਤੇ ਇਤਿਹਾਸਕ ਬਿਰਤਾਂਤ, ਸਥਾਨਕ ਲੋਕਧਾਰਾ ਬੋਲਦੇ ਹੋਏ।",
    chap3Num: "ਅਧਿਆਇ 03", chap3Title: "ਲਿਪਿਕਾ ਸਕ੍ਰਿਪਟ ਲੈਂਸ", chap3Desc: "ਪ੍ਰਾਚੀਨ ਬ੍ਰਾਹਮੀ, ਪਾਲੀ, ਅਤੇ ਸੰਸਕ੍ਰਿਤ ਪੱਥਰ ਦੇ ਸ਼ਿਲਾਲੇਖਾਂ ਨੂੰ ਡੀਕੋਡ ਕਰਨ ਵਾਲੀ ਰੀਅਲ-ਟਾਈਮ ਆਪਟੀਕਲ ਅੱਖਰ ਮਾਨਤਾ।",
    chap4Num: "ਅਧਿਆਇ 04", chap4Title: "ਬਲਾਕਚੈਨ ਯਾਤਰਾ ਪਾਸਪੋਰਟ", chap4Desc: "ਭਾਰਤ ਦੇ ਸੱਭਿਆਚਾਰਕ ਗਲਿਆਰਿਆਂ ਵਿੱਚ ਤੁਹਾਡੀ ਯਾਤਰਾ ਦੀ ਪੁਸ਼ਟੀ ਕਰਨ ਵਾਲੀਆਂ ਕ੍ਰਿਪਟੋਗ੍ਰਾਫਿਕ ਡਿਜੀਟਲ ਟਿਕਟਾਂ ਅਤੇ NFT।",
    chap5Num: "ਅਧਿਆਇ 05", chap5Title: "ਪ੍ਰਤੀਧਵਨੀ ਸਥਾਨਿਕ ਆਡੀਓ", chap5Desc: "3D ਬਾਇਨੋਰਲ ਧੁਨੀ ਪੁਨਰ ਨਿਰਮਾਣ ਜੋ ਕਲਾਸੀਕਲ ਸ਼ਾਹੀ ਅਦਾਲਤਾਂ ਅਤੇ ਮੰਦਰ ਦੇ ਮੰਤਰਾਂ ਦੀ ਨਕਲ ਕਰਦਾ ਹੈ।",
    chap6Num: "ਅਧਿਆਇ 06", chap6Title: "ਵਿਅੰਜਨ ਮਸਾਲਾ ਰੂਟ ਸਕੈਨਰ", chap6Desc: "AI ਕੰਪਿਊਟਰ ਵਿਜ਼ਨ ਖੇਤਰੀ ਰਸੋਈ ਇਤਿਹਾਸ ਅਤੇ ਪ੍ਰਾਚੀਨ ਮਸਾਲਾ ਵਪਾਰ ਦੇ ਮੂਲ ਨੂੰ ਡੀਕੋਡ ਕਰਦਾ ਹੈ।",
    chap7Num: "ਅਧਿਆਇ 07", chap7Title: "ਪਰੰਪਰਾ ਇਤਿਹਾਸ ਦੀਆਂ ਖੋਜਾਂ", chap7Desc: "ਜੀਓ-ਫੈਂਸਡ ਗੇਮੀਫਾਈਡ ਹੈਰੀਟੇਜ ਖੋਜ ਡਿਜੀਟਲ ਇਨਾਮ ਅਤੇ ਸਥਾਨਕ ਕਾਰੀਗਰਾਂ ਦੀਆਂ ਛੋਟਾਂ ਨੂੰ ਅਨਲੌਕ ਕਰਦੀ ਹੈ।",
    chap8Num: "ਅਧਿਆਇ 08", chap8Title: "ਕਲਾ-ਕ੍ਰਿਤੀ ਡਿਜੀਟਲ ਟਰਾਈ-ਆਨ", chap8Desc: "AR ਸਰੀਰ ਵਿਭਾਜਨ ਜੋ ਸੈਲਾਨੀਆਂ ਨੂੰ ਡਿਜ਼ੀਟਲ ਤੌਰ 'ਤੇ ਖੇਤਰੀ ਹੈਂਡਲੂਮ ਟੈਕਸਟਾਈਲ ਪਹਿਨਣ ਦੀ ਆਗਿਆ ਦਿੰਦਾ ਹੈ।",
    getStarted: "ਸ਼ੁਰੂ ਕਰੋ",
    authClose: "[ ਬੰਦ ਕਰੋ ]", authWork: "ਕੰਮ", authStudio: "ਸਟੂਡੀਓ", authLab: "ਲੈਬ", authContact: "ਸੰਪਰਕ",
    authJoinTitle: "ਵਿਰਾਸਤ ਵਿੱਚ ਸ਼ਾਮਲ ਹੋਵੋ", authJoinDesc: "ਵਿਅਕਤੀਗਤ ਵਿਰਾਸਤੀ ਯਾਤਰਾਵਾਂ ਅਤੇ ਕਲਾਕ੍ਰਿਤੀਆਂ ਨੂੰ ਅਨਲੌਕ ਕਰੋ।", authEmail: "ਈਮੇਲ ਪਤਾ",
    authCookieTitle: "ਬਹੁਤ ਸਮਾਂ ਪਹਿਲਾਂ, ਇੱਕ ਬਹੁਤ ਦੂਰ ਦੇ ਬ੍ਰਾਊਜ਼ਰ ਵਿੱਚ...", authCookieDesc: "ਅਸੀਂ ਇਹ ਸਮਝਣ ਲਈ ਕੂਕੀਜ਼ ਦੀ ਵਰਤੋਂ ਕਰਦੇ ਹਾਂ ਕਿ ਸਾਈਟ ਕਿਵੇਂ ਪ੍ਰਦਰਸ਼ਨ ਕਰਦੀ ਹੈ - ਕੁਝ ਵੀ ਭਿਆਨਕ ਨਹੀਂ, ਕੋਈ ਡਾਰਕ ਸਾਈਡ ਸਮੱਗਰੀ ਨਹੀਂ।",
    authCookieAccept: "ਕੂਕੀਜ਼ ਤੁਹਾਡੇ ਨਾਲ ਹੋਣ", authCookieDecline: "ਮੈਂ ਪਾਸ ਕਰਾਂਗਾ, ਧੰਨਵਾਦ",
    authPrivacy: "ਗੋਪਨੀਯਤਾ ਨੀਤੀ", authSupport: "ਸਹਾਇਤਾ"
  },
  hr: {
    logo: "भारत", navHome: "मुख्य पृष्ठ", navAbout: "म्हारै बारे म", navGallery: "गैलरी", navFeatures: "खासियत", heroTitle: "विरासत",
    introText: "पुराणे पत्थरां, चौड़े नज़ारे, अर कदे ना टूटन आली जीवंत परंपराओं का एक सुथरा ढांचा, जो भारतीय उपमहाद्वीप में एक गज़ब की सांस्कृतिक यात्रा नै आकार दे सै।",
    tag1: "पुराणी विरासत", tag2: "जीवित कला", tag3: "आध्यात्मिक यात्रा",
    panel1Title: "पत्थर जो बोलैं सैं।", panel1Desc: "भारत के स्मारक वास्तुकला के कंपास सैं जो कला, खगोल विज्ञान अर राजवंशीय इतिहास के हज़ारों सालां नै जोड़ैं सैं।",
    panel2Title: "जीवंत धड़कन।", panel2Desc: "उरै संस्कृति खाली म्यूज़ियम में कोन्या देखी जाती; या हर गली, त्योहार अर कारीगर की कला में महसूस करी जावै सै।",
    card1Kicker: "सदाबहार वास्तुकला", card1Title: "हम्पी के मंदिर", card1Desc: "विजयनगर साम्राज्य की शान नै दिखांदे पत्थर के रथ अर अखंड खंडहर।",
    card2Kicker: "आध्यात्मिक दिल", card2Title: "वाराणसी के घाट", card2Desc: "दुनिया का सबतै पुराणा जीवित शहर, जड़ै पुराणे रीति-रिवाज़ पवित्र गंगा तै मिलैं सैं।",
    card3Kicker: "शाही विरासत", card3Title: "राजपूताना किले", card3Desc: "राजस्थान के ना टूटन आले पहाड़ी किले जिन्मै सुथरे महल अर गज़ब के नज़ारे सैं।",
    card4Kicker: "जीवित कला", card4Title: "शास्त्रीय परंपराएं", card4Desc: "भरतनाट्यम तै लेकै पुराणे तांबे के शिल्प तक, एक इसी संस्कृति जो आज भी ज़िंदा सै अर सांस लेवै सै।",
    card5Kicker: "प्राकृतिक विरासत", card5Title: "केरल बैकवाटर्स", card5Desc: "पुराणे व्यापार रास्तों अर शांत पर्यावरण का आपस में जुड़्या होया नेटवर्क।",
    chaptersSubtitle: "विरासत की यात्रा", chaptersTitle: "अध्याय देख्याँ", exploreBtn: "देखें →",
    chap1Num: "अध्याय 01", chap1Title: "स्मारकां का पुरालेख", chap1Desc: "भारत के वास्तुशिल्प चमत्कारां का इंटरैक्टिव 3डी संरचनात्मक मानचित्रण और ऐतिहासिक पुनर्निर्माण।",
    chap2Num: "अध्याय 02", chap2Title: "कथाकार एआई गाइड", chap2Desc: "गूगल जेमिनी द्वारा संचालित संवादी विद्या अर ऐतिहासिक आख्यान, जो स्थानीय लोककथा बोलै सै।",
    chap3Num: "अध्याय 03", chap3Title: "लिपि स्क्रिप्ट लेंस", chap3Desc: "प्राचीन ब्राह्मी, पाली अर संस्कृत पत्थर के शिलालेखों ने डिकोड करण आली रियल-टाइम ऑप्टिकल चरित्र मान्यता।",
    chap4Num: "अध्याय 04", chap4Title: "ब्लॉकचेन यात्रा पासपोर्ट", chap4Desc: "भारत के सांस्कृतिक गलियारां म थारी यात्रा की पुष्टि करण आले क्रिप्टोग्राफिक डिजिटल टिकट अर एनएफटी।",
    chap5Num: "अध्याय 05", chap5Title: "प्रतिध्वनि स्थानिक ऑडियो", chap5Desc: "3डी बाइनॉरल ध्वनिक पुनर्निर्माण जो शाही दरबारां अर मंदिर के मंत्रां की नकल करै सै।",
    chap6Num: "अध्याय 06", chap6Title: "व्यंजन मसाला मार्ग स्कैनर", chap6Desc: "एआई कंप्यूटर विज़न क्षेत्रीय पकवान के इतिहास अर प्राचीन मसाला व्यापार नै डिकोड करै सै।",
    chap7Num: "अध्याय 07", chap7Title: "परंपरा विद्या अन्वेषण", chap7Desc: "जियो-फेंस्ड गामिफाइड हेरिटेज एक्सप्लोरेशन डिजिटल रिवॉर्ड्स अर स्थानीय कारीगरां की छूट नै अनलॉक करै सै।",
    chap8Num: "अध्याय 08", chap8Title: "कला-कृति डिजिटल ट्राई-ऑन", chap8Desc: "एआर बॉडी सेगमेंटेशन जिसतै पर्यटक क्षेत्रीय हथकरघा कपड़ां नै डिजिटल रूप तै पहण सकैं सैं।",
    getStarted: "शुरू करां",
    authClose: "[ बंद कर ]", authWork: "काम", authStudio: "स्टूडियो", authLab: "लैब", authContact: "संपर्क",
    authJoinTitle: "विरासत तै जुड़", authJoinDesc: "अपणी विरासत की यात्रा अर कलाकृतियां नै अनलॉक कर।", authEmail: "ईमेल एड्रेस",
    authCookieTitle: "घणे दिन पैहल्या, एक दूर के ब्राउज़र म...", authCookieDesc: "साइट किसा चालै सै, न्यू समझण खातिर हम कुकीज़ का इस्तेमाल करां सैं - कोई बुरी बात कोन्या, कोई डार्क साइड का सौदा कोन्या।",
    authCookieAccept: "कुकीज़ थारै गेल रैं", authCookieDecline: "मैं ना ल्यूं, धन्यवाद",
    authPrivacy: "प्राइवेसी पॉलिसी", authSupport: "मदद"
  }
};

function switchLanguage(langCode) {
  document.getElementById('current-lang').innerText = langCode.toUpperCase();
  const dict = translations[langCode];
  
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const key = el.getAttribute('data-i18n');
    if(dict[key]) {
      if (el.tagName === 'INPUT' && el.hasAttribute('placeholder')) {
        el.setAttribute('placeholder', dict[key]);
      } else {
        el.innerText = dict[key];
      }
    }
  });
}

window.addEventListener("load", () => {
  const loader = document.getElementById("app-loading-container");
  if (loader) {
    setTimeout(() => {
      loader.classList.add("is-hidden");
    }, 1500); // Increased to 1500ms so it is definitely noticeable
  }
});

document.addEventListener("DOMContentLoaded", () => {
  document.querySelectorAll('.language-menu button').forEach(btn => {
    btn.addEventListener('click', (e) => {
      switchLanguage(e.target.getAttribute('data-lang'));
    });
  });

  const root = document.documentElement;
  const cinemaScroll = document.querySelector(".cinema-scroll");
  const sightsTrack = document.querySelector(".sights-track");
  const sightsControls = document.querySelector(".sights-controls");
  const sightPrev = document.querySelector(".sight-prev");
  const sightNext = document.querySelector(".sight-next");
  const originalSightCards = Array.from(document.querySelectorAll(".sight-card"));
  
  const reduceMotion = matchMedia("(prefers-reduced-motion: reduce)");

  let targetMouseX = 0, targetMouseY = 0;
  let mouseX = 0, mouseY = 0;
  let targetScroll = 0, smoothScroll = 0;
  let initialized = false;
  let rafPending = false;
  
  let sightCards = [];
  let originalSightCount = originalSightCards.length;
  let activeSight = originalSightCount; 

  const clamp = (v, min = 0, max = 1) => Math.min(max, Math.max(min, v));
  const smoothstep = (e0, e1, v) => { const x = clamp((v - e0) / (e1 - e0)); return x * x * (3 - 2 * x); };
  const lerp = (a, b, t) => a + (b - a) * t;
  const segmentInOut = (s, a, b, c, d) => {
    const enter = smoothstep(a, b, s), exit = smoothstep(c, d, s);
    return { enter, exit, active: enter * (1 - exit) };
  };
  const getScrollDistance = () => clamp(-cinemaScroll.getBoundingClientRect().top, 0, cinemaScroll.offsetHeight - window.innerHeight);

  const update = () => {
    rafPending = false;
    
    targetScroll = getScrollDistance();
    if (!initialized || reduceMotion.matches) { 
      smoothScroll = targetScroll; 
      initialized = true; 
    } else { 
      smoothScroll = lerp(smoothScroll, targetScroll, 0.14); 
    }
    if (Math.abs(smoothScroll - targetScroll) < 0.08) smoothScroll = targetScroll;

    mouseX = lerp(mouseX, targetMouseX, 0.12);
    mouseY = lerp(mouseY, targetMouseY, 0.12);

    const frame2 = segmentInOut(smoothScroll, 560, 900, 1300, 1620);
    const frame3 = segmentInOut(smoothScroll, 1760, 2140, 2540, 2700);
    const progress = clamp(smoothScroll / 2700);
    const introExit = smoothstep(90, 650, smoothScroll);
    const sightsEnterRaw = smoothstep(2760, 3560, smoothScroll);
    const sightsEnter = Math.pow(sightsEnterRaw, 1.55);
    const sightsControlsEnter = smoothstep(3360, 3660, smoothScroll);
    const blurActive = clamp(frame2.active + frame3.active);
    const frame2Opacity = frame2.active * (1 - frame3.enter);
    const splitDrift = Math.pow(frame2.enter, 1.5);
    const panel2Opacity = frame2.active * (1 - frame2.exit);
    const panel3Opacity = frame3.active * (1 - frame3.exit);
    const backScale = 0.76 + progress * 0.2 + frame2.enter * 0.18 + frame3.enter * 0.16;
    const sharedHeroY = progress * -74;
    const sharedHeroScale = progress * 0.23;
    const sightsScreenTop = Math.min(220, Math.max(112, window.innerHeight * 0.19)) - 50;
    const sightsParentTop = window.innerHeight - (window.innerHeight - sightsScreenTop) / backScale;

    root.style.setProperty("--mx", reduceMotion.matches ? 0 : mouseX.toFixed(4));
    root.style.setProperty("--my", reduceMotion.matches ? 0 : mouseY.toFixed(4));

    root.style.setProperty("--back-opacity", (1 - frame2.active * 0.06).toFixed(4));
    root.style.setProperty("--back-x", `${(mouseX * -12).toFixed(4)}px`);
    root.style.setProperty("--back-y", `${(mouseY * -4).toFixed(4)}px`);
    root.style.setProperty("--back-scale", backScale.toFixed(4));
    root.style.setProperty("--four-y", `${(10 + progress * 10).toFixed(4)}vh`);
    root.style.setProperty("--four-scale", (0.78 + progress * 0.16).toFixed(4));
    root.style.setProperty("--bazaar-y", `${(20 - progress * 8).toFixed(4)}vh`);
    root.style.setProperty("--blur-px", `${(blurActive * 14).toFixed(4)}px`);
    root.style.setProperty("--back-brightness", (1 - blurActive * 0.255).toFixed(4));
    root.style.setProperty("--bazaar-blur-px", `${(frame2.active * 14).toFixed(4)}px`);
    root.style.setProperty("--bazaar-brightness", (1 - frame2.active * 0.255 - frame3.active * 0.06).toFixed(4));
    root.style.setProperty("--bazaar-saturation", (1 + frame3.active * 0.18).toFixed(4));
    root.style.setProperty("--shade-opacity", "1");
    root.style.setProperty("--shade-z", frame2.active > 0.02 ? "2" : "0");
    root.style.setProperty("--shade-top-alpha", (blurActive * 0.465).toFixed(4));
    root.style.setProperty("--shade-mid-alpha", (blurActive * 0.42).toFixed(4));
    root.style.setProperty("--shade-bottom-alpha", (blurActive * 0.51).toFixed(4));

    root.style.setProperty("--title-y", `${(introExit * -210).toFixed(4)}px`);
    root.style.setProperty("--title-scale", (1 - introExit * 0.08).toFixed(4));
    root.style.setProperty("--title-opacity", (1 - introExit).toFixed(4));

    root.style.setProperty("--monument-x", `calc(-50% + ${(mouseX * 18).toFixed(4)}px)`);
    root.style.setProperty("--monument-y", `${(mouseY * 8 + sharedHeroY - frame2.exit * 760).toFixed(4)}px`);
    root.style.setProperty("--monument-bottom", `${(5 - frame2.enter * 13).toFixed(4)}vh`);
    root.style.setProperty("--monument-width", `${(67.2 + frame2.enter * 37.8).toFixed(4)}vw`);
    root.style.setProperty("--monument-scale", (1.02 + sharedHeroScale + frame2.exit * 0.46).toFixed(4));

    root.style.setProperty("--split-left-x", `calc(-50% + ${(-splitDrift * 46).toFixed(4)}vw + ${(mouseX * 22).toFixed(4)}px)`);
    root.style.setProperty("--split-left-y", `${(mouseY * 10 + sharedHeroY - splitDrift * 180).toFixed(4)}px`);
    root.style.setProperty("--split-left-scale", (1 + sharedHeroScale + frame2.enter * 0.74).toFixed(4));
    root.style.setProperty("--split-right-x", `calc(-50% + ${(splitDrift * 46).toFixed(4)}vw + ${(mouseX * 22).toFixed(4)}px)`);
    root.style.setProperty("--split-right-y", `${(mouseY * 10 + sharedHeroY - splitDrift * 180).toFixed(4)}px`);
    root.style.setProperty("--split-right-scale", (1 + sharedHeroScale + frame2.enter * 0.74).toFixed(4));

    root.style.setProperty("--frame2-opacity", frame2Opacity.toFixed(4));
    root.style.setProperty("--frame2-x", `calc(-50% + ${(mouseX * 10).toFixed(4)}px)`);
    root.style.setProperty("--frame2-y", `calc(-50% + ${(mouseY * 8 - frame2.exit * 150).toFixed(4)}px)`);
    root.style.setProperty("--frame2-scale", (1.06 + frame2.enter * 0.08 + frame2.exit * 0.08).toFixed(4));

    root.style.setProperty("--intro-copy-y", `${(introExit * 90).toFixed(4)}px`);
    root.style.setProperty("--intro-copy-opacity", (1 - introExit).toFixed(4));
    root.style.setProperty("--panel2-opacity", panel2Opacity.toFixed(4));
    root.style.setProperty("--panel2-y", `calc(-50% + ${(-frame2.exit * 86 + (1 - frame2.enter) * 58).toFixed(4)}px)`);
    root.style.setProperty("--panel3-opacity", panel3Opacity.toFixed(4));
    root.style.setProperty("--panel3-y", `calc(-50% + ${(-frame3.exit * 86 + (1 - frame3.enter) * 58).toFixed(4)}px)`);

    root.style.setProperty("--sights-opacity", sightsEnter.toFixed(4));
    root.style.setProperty("--sights-controls-opacity", sightsControlsEnter.toFixed(4));
    sightsControls.classList.toggle("is-ready", sightsControlsEnter > 0.98);
    root.style.setProperty("--sights-visibility", sightsEnter > 0.01 ? "visible" : "hidden");
    root.style.setProperty("--sights-y", "0px");
    root.style.setProperty("--sights-enter-x", `${((1 - sightsEnter) * 420).toFixed(4)}vw`);
    root.style.setProperty("--sights-scale", (1 / backScale).toFixed(4));
    root.style.setProperty("--sights-top", `${sightsParentTop.toFixed(4)}px`);
    root.style.setProperty("--sights-screen-top", `${sightsScreenTop.toFixed(4)}px`);

    if (Math.abs(smoothScroll - targetScroll) > 0.08 || 
        Math.abs(mouseX - targetMouseX) > 0.001 || 
        Math.abs(mouseY - targetMouseY) > 0.001) {
      requestTick();
    }
  };

  const requestTick = () => {
    if (!rafPending) {
      rafPending = true;
      requestAnimationFrame(update);
    }
  };

  const setupSightSlider = () => {
    sightsTrack.replaceChildren();
    for (let setIndex = 0; setIndex < 3; setIndex++) {
      originalSightCards.forEach((card, cardIndex) => {
        const clone = card.cloneNode(true);
        clone.dataset.sightIndex = setIndex * originalSightCount + cardIndex;
        sightsTrack.appendChild(clone);
      });
    }
    sightCards = Array.from(sightsTrack.children);
    activeSight = originalSightCount;

    sightCards.forEach(card => {
      card.addEventListener("click", () => selectSightCard(card));
      card.addEventListener("keydown", (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          selectSightCard(card);
        }
      });
    });

    sightsTrack.addEventListener("transitionend", normalizeSightSlider);
    updateSightSlider();
  };

  const updateSightSlider = () => {
    if (sightCards.length === 0) return;
    const cardWidth = sightCards[0].offsetWidth;
    const gap = parseFloat(getComputedStyle(sightsTrack).columnGap || "0");
    root.style.setProperty("--sights-shift", `${-(cardWidth + gap) * activeSight}px`);
    
    sightCards.forEach(card => {
      if (Number(card.dataset.sightIndex) === activeSight) {
        card.classList.add("is-active");
      } else {
        card.classList.remove("is-active");
      }
    });
  };

  const moveSightSlider = (dir) => {
    activeSight += dir;
    updateSightSlider();
  };

  const selectSightCard = (card) => {
    const idx = Number(card.dataset.sightIndex);
    if (isFinite(idx)) {
      activeSight = idx;
      updateSightSlider();
    }
  };

  const jumpSightSlider = (i) => {
    sightsTrack.classList.add("is-jumping");
    activeSight = i;
    updateSightSlider();
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        sightsTrack.classList.remove("is-jumping");
      });
    });
  };

  const normalizeSightSlider = () => {
    if (activeSight >= originalSightCount * 2) {
      jumpSightSlider(activeSight - originalSightCount);
    } else if (activeSight < originalSightCount) {
      jumpSightSlider(activeSight + originalSightCount);
    }
  };

  window.addEventListener("scroll", requestTick, { passive: true });
  window.addEventListener("resize", () => {
    updateSightSlider();
    requestTick();
  });
  window.addEventListener("pointermove", (e) => {
    targetMouseX = e.clientX / window.innerWidth - 0.5;
    targetMouseY = e.clientY / window.innerHeight - 0.5;
    requestTick();
  }, { passive: true });

  sightPrev.addEventListener("click", () => moveSightSlider(-1));
  sightNext.addEventListener("click", () => moveSightSlider(1));

  setupSightSlider();
  requestTick();
});

// Chapters Modal Interaction Logic
const openChaptersBtn = document.getElementById('open-chapters');
const closeChaptersBtn = document.getElementById('close-chapters');
const chaptersModal = document.getElementById('chapters-modal');

if (openChaptersBtn && chaptersModal) {
  openChaptersBtn.addEventListener('click', () => {
    chaptersModal.classList.add('is-open');
    document.body.style.overflow = 'hidden'; // Lock background scroll
  });
}

if (closeChaptersBtn && chaptersModal) {
  closeChaptersBtn.addEventListener('click', () => {
    chaptersModal.classList.remove('is-open');
    document.body.style.overflow = ''; // Restore scroll
  });
}

// Close on clicking outside container
chaptersModal.addEventListener('click', (e) => {
  if (e.target === chaptersModal) {
    chaptersModal.classList.remove('is-open');
    document.body.style.overflow = '';
  }
});

// Wire Chapter 1 Click Action
document.querySelectorAll('.chapter-card').forEach(card => {
  card.addEventListener('click', () => {
    const chapterId = card.getAttribute('data-chapter');
    
    // Close main chapters modal
    const chaptersModal = document.getElementById('chapters-modal');
    if (chaptersModal) chaptersModal.classList.remove('is-open');

    if (chapterId === '1') {
      const feat1Modal = document.getElementById('feature-1-modal');
      if (feat1Modal) feat1Modal.classList.add('is-open');
    } else if (chapterId === '2') {
      const feat2Modal = document.getElementById('feature-2-modal');
      if (feat2Modal) feat2Modal.classList.add('is-open');
    } else if (chapterId === '3') {
      const ch3View = document.getElementById('fullscreen-chapter-03');
      if (ch3View) ch3View.classList.add('is-active');
      document.body.style.overflow = 'hidden';
    } else if (chapterId === '5') {
      const ch5View = document.getElementById('fullscreen-chapter-05');
      if (ch5View) ch5View.classList.add('is-active');
      document.body.style.overflow = 'hidden';
    } else if (chapterId === '6') {
      const ch6View = document.getElementById('fullscreen-chapter-06');
      if (ch6View) ch6View.classList.add('is-active');
      document.body.style.overflow = 'hidden';
    } else {
      alert(`Launching Virasat Feature Module: Chapter 0${chapterId} - Initializing immersive ecosystem...`);
    }
  });
});

// Close Feature 1 Modal
const closeFeat1Btn = document.getElementById('close-feature-1');
const feat1Modal = document.getElementById('feature-1-modal');
if (closeFeat1Btn && feat1Modal) {
  closeFeat1Btn.addEventListener('click', () => {
    feat1Modal.classList.remove('is-open');
    document.body.style.overflow = '';
  });
}

// Map monument names to real high-res verified image URLs with guaranteed CORS
const monumentImages = {
  "hampi chariot": "images/hampi.png",
  "konark sun temple": "images/konark.png",
  "brihadeeswarar temple": "images/brihadeeswarar.png",
  "qutb minar tower": "images/qutb.png",
  "rani ki vav stepwell": "images/stepwell.png",
  "taj mahal mausoleum": "images/taj.png"
};

function updateSpotlightImage(queryKey) {
  const normalized = queryKey.toLowerCase().trim();
  let foundUrl = monumentImages["taj mahal mausoleum"]; // Default fallback
  
  for (let key in monumentImages) {
    if (normalized.includes(key)) {
      foundUrl = monumentImages[key];
      break;
    }
  }

  const imgEl = document.getElementById('spotlight-img');
  if (imgEl) {
    imgEl.src = foundUrl;
  }
}

async function fetchMonumentDataFromGemini(monumentName) {
  const info = document.getElementById('wireframe-info');
  if (info) {
    info.innerHTML = `<strong>[QUERYING GEMINI KNOWLEDGE BASE...]</strong><br>Fetching verified historical telemetry for ${monumentName}...`;
  }

  const prompt = `Provide precise, verified historical facts about the Indian monument "${monumentName}". Return ONLY a JSON object with these exact keys: "name", "dynasty", "location", "heightOrSpan", "material", and "verifiedFact". No markdown wrappers outside JSON.`;

  try {
    const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent?key=${GEMINI_API_KEY}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        contents: [{ parts: [{ text: prompt }] }]
      })
    });

    const data = await response.json();
    const rawText = data.candidates[0].content.parts[0].text;
    
    // Clean up markdown code blocks if Gemini returns them
    const cleanJson = rawText.replace(/```json/g, '').replace(/```/g, '').trim();
    const result = JSON.parse(cleanJson);

    if (info) {
      info.innerHTML = `
        <strong style="color: #ffcc00;">[VERIFIED BY GEMINI AI: ${result.name.toUpperCase()}]</strong><br>
        📍 Location: ${result.location} | Era: ${result.dynasty}<br>
        🏛️ Specs: ${result.heightOrSpan} | Material: ${result.material}<br>
        <span style="color: #4bb543;">✓ Fact: ${result.verifiedFact}</span>
      `;
    }
  } catch (error) {
    console.error("Gemini API Error:", error);
    if (info) {
      info.innerHTML = `<span style="color: #ff4d4d;">⚠️ Failed to fetch live data. Please verify your Gemini API key.</span>`;
    }
  }
}

// Hook up preset monument buttons
document.querySelectorAll('.inspect-btn').forEach((btn) => {
  btn.addEventListener('click', (e) => {
    const parent = e.target.closest('.monument-item');
    const name = parent.getAttribute('data-name');
    
    updateSpotlightImage(name);
    fetchMonumentDataFromGemini(name);
  });
});

// Auth / Menu Modal Logic (Produx Inspired)
const openAuthBtn = document.getElementById('open-auth');
const closeAuthBtn = document.getElementById('close-auth');
const authModal = document.getElementById('auth-modal');

if (openAuthBtn && authModal) {
  openAuthBtn.addEventListener('click', () => {
    authModal.classList.add('is-open');
    document.body.style.overflow = 'hidden';
  });
}

if (closeAuthBtn && authModal) {
  closeAuthBtn.addEventListener('click', () => {
    authModal.classList.remove('is-open');
    document.body.style.overflow = '';
  });
}

// Close Auth Modal on clicking outside content area
if (authModal) {
  authModal.addEventListener('click', (e) => {
    if (e.target === authModal) {
      authModal.classList.remove('is-open');
      document.body.style.overflow = '';
    }
  });
}

// Custom Monument Procedural 3D Mesh Synthesis Logic
const customInput = document.getElementById('custom-monument-input');
const customBtn = document.getElementById('custom-monument-btn');

if (customBtn && customInput) {
  customBtn.addEventListener('click', () => {
    const val = customInput.value.trim();
    if (!val) return;

    updateSpotlightImage(val);
    fetchMonumentDataFromGemini(val);
    customInput.value = '';
  });

  customInput.addEventListener('keypress', (e) => {
    if (e.key === 'Enter') customBtn.click();
  });
}

// Close Feature 2 Modal
const closeFeat2Btn = document.getElementById('close-feature-2');
const feat2Modal = document.getElementById('feature-2-modal');
if (closeFeat2Btn && feat2Modal) {
  closeFeat2Btn.addEventListener('click', () => {
    feat2Modal.classList.remove('is-open');
    document.body.style.overflow = '';
  });
}

const chatMessages = document.getElementById('chat-messages');
const chatInput = document.getElementById('chat-input');
const sendChatBtn = document.getElementById('send-chat-btn');

let chatSessions = [
  { id: 1, title: "Hampi Chariot Legend", messages: [{ sender: 'ai', text: "Namaste, traveler. I am Kathakar. Ask me about the legends of Hampi, the secrets of the Chola temples, or any monument on your journey." }] }
];
let activeSessionId = 1;

const chatHistoryList = document.getElementById('chat-history-list');
const newChatBtn = document.getElementById('new-chat-btn');

function renderHistoryList() {
  if (!chatHistoryList) return;
  chatHistoryList.innerHTML = '';
  chatSessions.forEach(session => {
    const btn = document.createElement('button');
    btn.className = `history-item ${session.id === activeSessionId ? 'active' : ''}`;
    btn.textContent = session.title;
    btn.addEventListener('click', () => {
      activeSessionId = session.id;
      loadActiveSession();
      renderHistoryList();
    });
    chatHistoryList.appendChild(btn);
  });
}

function loadActiveSession() {
  if (!chatMessages) return;
  chatMessages.innerHTML = '';
  const currentSession = chatSessions.find(s => s.id === activeSessionId);
  if (currentSession) {
    currentSession.messages.forEach(msg => {
      const msgDiv = document.createElement('div');
      msgDiv.className = `chat-msg ${msg.sender === 'ai' ? 'ai-msg' : 'user-msg'}`;
      msgDiv.innerHTML = `<span>${msg.text}</span>`;
      chatMessages.appendChild(msgDiv);
    });
    chatMessages.scrollTop = chatMessages.scrollHeight;
  }
}

if (newChatBtn) {
  newChatBtn.addEventListener('click', () => {
    const newId = Date.now();
    chatSessions.unshift({
      id: newId,
      title: "New Heritage Inquiry",
      messages: [{ sender: 'ai', text: "Namaste! What new historical trail shall we explore today?" }]
    });
    activeSessionId = newId;
    loadActiveSession();
    renderHistoryList();
  });
}

// Update message append logic to persist into active session history
async function sendQueryToKathakar(query) {
  if (!query.trim()) return;
  
  const currentSession = chatSessions.find(s => s.id === activeSessionId);
  if (currentSession && currentSession.title === "New Heritage Inquiry") {
    currentSession.title = query.length > 22 ? query.substring(0, 22) + "..." : query;
  }

  currentSession.messages.push({ sender: 'user', text: query });
  loadActiveSession();
  chatInput.value = '';
  renderHistoryList();

  const loadingId = 'loading-' + Date.now();
  const loadingDiv = document.createElement('div');
  loadingDiv.className = 'chat-msg ai-msg';
  loadingDiv.id = loadingId;
  loadingDiv.innerHTML = `<span><em>Kathakar is consulting the ancient archives...</em></span>`;
  chatMessages.appendChild(loadingDiv);
  chatMessages.scrollTop = chatMessages.scrollHeight;

  const systemPrompt = "You are Kathakar, an immersive, eloquent AI historical storyteller for the 'Virasat' Indian heritage platform. Speak with narrative depth, cultural warmth, and 100% historical accuracy regarding Indian monuments, dynasties, and folklore.";

  try {
    const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent?key=${GEMINI_API_KEY}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        contents: [
          { role: "user", parts: [{ text: systemPrompt + "\n\nUser Question: " + query }] }
        ]
      })
    });

    const data = await response.json();
    const replyText = data.candidates[0].content.parts[0].text;

    document.getElementById(loadingId)?.remove();
    currentSession.messages.push({ sender: 'ai', text: replyText });
    loadActiveSession();
  } catch (error) {
    console.error("Kathakar API Error:", error);
    document.getElementById(loadingId)?.remove();
    const errorMsg = "Forgive me, traveler. The spiritual network encountered an interruption. Please verify your Gemini API key.";
    currentSession.messages.push({ sender: 'ai', text: errorMsg });
    loadActiveSession();
  }
}

if (sendChatBtn && chatInput) {
  sendChatBtn.addEventListener('click', () => sendQueryToKathakar(chatInput.value));
  chatInput.addEventListener('keypress', (e) => {
    if (e.key === 'Enter') sendQueryToKathakar(chatInput.value);
  });
}

document.querySelectorAll('.chip').forEach(chip => {
  chip.addEventListener('click', (e) => {
    sendQueryToKathakar(e.target.getAttribute('data-query'));
  });
});

// Initial render call on load
renderHistoryList();



// Full-Screen Chapter 05: Tirth-Yatra Forecast Logic
const ch5View = document.getElementById('fullscreen-chapter-05');
const backCh5Btn = document.getElementById('back-from-ch5');
const analyzeBtn = document.getElementById('ch5-analyze-btn');
const searchInput = document.getElementById('ch5-site-search');

if (backCh5Btn) {
  backCh5Btn.addEventListener('click', () => {
    ch5View.classList.remove('is-active');
    document.body.style.overflow = '';
  });
}

async function generateCrowdForecast(siteName) {
  if (!siteName.trim()) return;
  
  document.getElementById('ch5-density-output').textContent = "Analyzing...";
  document.getElementById('ch5-peak-output').textContent = "Scanning...";
  document.getElementById('ch5-optimal-output').textContent = "Calculating...";
  document.getElementById('ch5-density-bar').style.width = "0%";

  const prompt = `Act as an advanced predictive analytics AI for Indian pilgrimage tourism. Analyze the site: "${siteName}". Based on historical data, standard ritual times, and typical tourist behavior, provide a highly realistic JSON forecast.
  Return ONLY a JSON object with these exact keys:
  "densityStatus" (e.g. "Severe Congestion", "Moderate Footfall"),
  "densityPercentage" (a number between 10 and 100),
  "peakWindow" (e.g. "05:00 AM - 09:00 AM"),
  "peakReason" (Brief reason, e.g. "Morning Aarti and Darshan queue peak"),
  "optimalWindow" (e.g. "02:30 PM - 04:30 PM"),
  "optimalReason" (Brief reason, e.g. "Post-lunch lull in queues").
  No markdown wrappers.`;

  try {
    const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent?key=${GEMINI_API_KEY}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ contents: [{ parts: [{ text: prompt }] }] })
    });
    
    const data = await response.json();
    const result = JSON.parse(data.candidates[0].content.parts[0].text.replace(/```json/g, '').replace(/```/g, '').trim());

    document.getElementById('ch5-density-output').textContent = result.densityStatus;
    document.getElementById('ch5-density-bar').style.width = `${result.densityPercentage}%`;
    
    const barColor = result.densityPercentage > 80 ? '#ff4d4d' : (result.densityPercentage > 50 ? '#ffcc00' : '#4bb543');
    document.getElementById('ch5-density-bar').style.background = barColor;
    document.getElementById('ch5-density-output').style.color = barColor;

    document.getElementById('ch5-peak-output').textContent = result.peakWindow;
    document.getElementById('ch5-peak-reason').textContent = result.peakReason;
    document.getElementById('ch5-optimal-output').textContent = result.optimalWindow;
    document.getElementById('ch5-optimal-reason').textContent = result.optimalReason;
  } catch (error) {
    console.error("Forecast API Error:", error);
    document.getElementById('ch5-density-output').textContent = "API Error";
    document.getElementById('ch5-peak-output').textContent = "Check API Key";
  }
}

if (analyzeBtn && searchInput) {
  analyzeBtn.addEventListener('click', () => generateCrowdForecast(searchInput.value));
  searchInput.addEventListener('keypress', (e) => {
    if (e.key === 'Enter') generateCrowdForecast(searchInput.value);
  });
}

// Full-Screen Chapter 06: Kala-Bazaar Artisan Heritage
const ch6View = document.getElementById('fullscreen-chapter-06');
const backCh6Btn = document.getElementById('back-from-ch6');
const exploreBtn = document.getElementById('ch6-explore-btn');
const regionInput = document.getElementById('ch6-region-search');
const outputContainer = document.getElementById('ch6-output-container');

if (backCh6Btn) {
  backCh6Btn.addEventListener('click', () => {
    ch6View.classList.remove('is-active');
    document.body.style.overflow = '';
  });
}

// Dynamic Gemini API Fetch for Chapter 06
async function generateCraftTelemetry(regionName) {
  if (!regionName.trim()) return;
  
  // Show container and set loading states
  outputContainer.style.display = 'block';
  document.getElementById('ch6-region-title').textContent = "Scanning Archives...";
  document.getElementById('ch6-craft-name').textContent = "Analyzing region...";
  document.getElementById('ch6-craft-history').textContent = "Consulting archaeological and GI registries...";
  document.getElementById('ch6-auth-markers').innerHTML = "<li>Loading authentic verification steps...</li>";

  const prompt = `Act as an expert Indian cultural heritage and Geographical Indication (GI) AI. The user is asking about the region: "${regionName}". 
  Identify the most famous ancient handicraft, weave, or artisanal product from this specific region.
  Return ONLY a JSON object with these exact keys:
  "regionFormatted" (Cleanly formatted name of the region),
  "giStatus" (e.g. "GI Tagged in 2005" or "Traditional Heritage"),
  "craftName" (The name of the craft, e.g. "Banarasi Silk Brocade"),
  "craftHistory" (A 2-3 sentence rich historical context of the craft's origin),
  "authenticityMarkers" (An array of 3 strings detailing how a tourist can identify an authentic piece vs a fake).
  No markdown wrappers.`;

  try {
    const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent?key=${GEMINI_API_KEY}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ contents: [{ parts: [{ text: prompt }] }] })
    });
    
    const data = await response.json();
    
    if (!response.ok) {
      if (data.error && data.error.code === 429) {
        throw new Error("RATE LIMIT EXCEEDED");
      }
      throw new Error("API ERROR");
    }

    const result = JSON.parse(data.candidates[0].content.parts[0].text.replace(/```json/g, '').replace(/```/g, '').trim());

    // Update Dashboard UI
    document.getElementById('ch6-region-title').textContent = result.regionFormatted.toUpperCase();
    document.getElementById('ch6-gi-status').textContent = result.giStatus.toUpperCase();
    document.getElementById('ch6-craft-name').textContent = result.craftName;
    document.getElementById('ch6-craft-history').textContent = result.craftHistory;
    
    const markersUl = document.getElementById('ch6-auth-markers');
    markersUl.innerHTML = '';
    result.authenticityMarkers.forEach(marker => {
      const li = document.createElement('li');
      li.textContent = marker;
      li.style.marginBottom = "8px";
      markersUl.appendChild(li);
    });

  } catch (error) {
    console.error("Craft Forecast API Error:", error);
    if (error.message === "RATE LIMIT EXCEEDED") {
      document.getElementById('ch6-region-title').textContent = "RATE LIMIT HIT";
      document.getElementById('ch6-craft-history').textContent = "Google Gemini free tier quota exceeded (20 requests/min). Please wait 60 seconds and try again.";
    } else {
      document.getElementById('ch6-region-title').textContent = "NETWORK ERROR";
      document.getElementById('ch6-craft-history').textContent = "Failed to connect to the Gemini API. Please check your API key and network connection.";
    }
  }
}

if (exploreBtn && regionInput) {
  exploreBtn.addEventListener('click', () => generateCraftTelemetry(regionInput.value));
  regionInput.addEventListener('keypress', (e) => {
    if (e.key === 'Enter') generateCraftTelemetry(regionInput.value);
  });
}

const ch3View = document.getElementById('fullscreen-chapter-03');
const backCh3Btn = document.getElementById('back-from-ch3');
const ch3FileInput = document.getElementById('ch3-file-input');
const ch3Telemetry = document.getElementById('ch3-telemetry');
const ch3Laser = document.getElementById('ch3-laser');

if (backCh3Btn) {
  backCh3Btn.addEventListener('click', () => {
    ch3View.classList.remove('is-active');
    document.body.style.overflow = '';
  });
}

async function analyzeArtifactWithGemini(base64Data, mimeType) {
  // UI Loading State
  ch3Telemetry.style.opacity = '1';
  document.getElementById('ch3-era-output').textContent = "Scanning Morphology...";
  document.getElementById('ch3-script-type').textContent = "Identifying script family...";
  document.getElementById('ch3-glyph-output').textContent = "Extracting...";
  document.getElementById('ch3-translation-output').textContent = "Cross-referencing ancient lexicons...";
  document.getElementById('ch3-context-output').textContent = "Retrieving archaeological context...";

  // The prompt that differentiates this from Google Lens
  const prompt = `Act as an expert epigraphist and archaeologist specializing in ancient Indian history. Analyze the uploaded image of the inscription or manuscript. 
  Do not just translate it. Analyze the stroke morphology to estimate its era and dynasty. 
  Return ONLY a JSON object with these exact keys:
  "estimatedEra" (e.g., "3rd Century BCE - Mauryan Empire"),
  "scriptFamily" (e.g., "Ashokan Brahmi" or "Grantha Script"),
  "extractedGlyphs" (The raw text in the original ancient script or closest Unicode equivalent),
  "modernTranslation" (The English translation),
  "archaeologicalContext" (2-3 sentences explaining the historical significance of this specific type of edict/text).
  No markdown wrappers.`;

  try {
    const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-3.5-flash:generateContent?key=${GEMINI_API_KEY}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        contents: [{
          parts: [
            { text: prompt },
            { inline_data: { mime_type: mimeType, data: base64Data } }
          ]
        }]
      })
    });
    
    const data = await response.json();
    const result = JSON.parse(data.candidates[0].content.parts[0].text.replace(/```json/g, '').replace(/```/g, '').trim());

    // Update UI and stop laser
    ch3Laser.style.display = 'none';
    document.getElementById('ch3-era-output').textContent = result.estimatedEra.toUpperCase();
    document.getElementById('ch3-script-type').textContent = `SCRIPT MATCH: ${result.scriptFamily.toUpperCase()}`;
    document.getElementById('ch3-glyph-output').textContent = result.extractedGlyphs;
    document.getElementById('ch3-translation-output').textContent = `"${result.modernTranslation}"`;
    document.getElementById('ch3-context-output').textContent = result.archaeologicalContext;

  } catch (error) {
    console.error("Epigraphic API Error:", error);
    ch3Laser.style.display = 'none';
    document.getElementById('ch3-era-output').textContent = "ANALYSIS FAILED";
    document.getElementById('ch3-context-output').textContent = "Ensure the image is clear and your Gemini Vision API key is active.";
  }
}

if (ch3FileInput) {
  ch3FileInput.addEventListener('change', (e) => {
    const file = e.target.files[0];
    if (!file) return;

    const reader = new FileReader();
    reader.onload = function(uploadEvent) {
      const fullBase64 = uploadEvent.target.result;
      const mimeType = file.type;
      const base64Data = fullBase64.split(',')[1];

      // Update Image Preview & Start Laser
      document.getElementById('ch3-upload-placeholder').style.display = 'none';
      const previewImg = document.getElementById('ch3-preview-img');
      previewImg.src = fullBase64;
      previewImg.style.display = 'block';
      ch3Laser.style.display = 'block';

      // Send to Gemini
      analyzeArtifactWithGemini(base64Data, mimeType);
    };
    reader.readAsDataURL(file);
  });
}
